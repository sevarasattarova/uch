<Page x:Class="uch.Pages.CatalogPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:uch.Pages"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="CatalogPage" Background="White">

        <Grid Margin="20">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <TextBox x:Name="SearchBox" TextChanged="SearchBox_TextChanged" Margin="0,0,0,10" Padding="8" Tag="Поиск по названию или автору"/>

            <WrapPanel Grid.Row="1" Margin="0,0,0,10">
                <ComboBox x:Name="SortCombo" SelectionChanged="SortCombo_SelectionChanged" Width="150" Margin="0,0,10,0">
                <ComboBoxItem Content="По названию"/>
                    <ComboBoxItem Content="По рейтингу"/>
                </ComboBox>
                <ComboBox x:Name="GenreFilter" SelectionChanged="GenreFilter_SelectionChanged" Width="150" DisplayMemberPath="Name"/>
                <Button Content="Сбросить фильтр" Click="ResetFilter_Click" Margin="10,0,0,0" Padding="8,4"/>
            </WrapPanel>

            <ScrollViewer Grid.Row="2" VerticalScrollBarVisibility="Auto">
                <ItemsControl x:Name="BooksGrid">
                    <ItemsControl.ItemsPanel>
                        <ItemsPanelTemplate>
                            <WrapPanel/>
                        </ItemsPanelTemplate>
                    </ItemsControl.ItemsPanel>
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Border BorderBrush="#CCC" BorderThickness="1" CornerRadius="8" Width="200" Margin="10" Padding="10" Background="White">
                                <StackPanel>
                                    <Image Source="{Binding CoverImageUrl}" Height="150" Stretch="UniformToFill"/>
                                    <TextBlock Text="{Binding Title}" FontWeight="Bold" FontSize="16" TextWrapping="Wrap" Margin="0,5"/>
                                    <TextBlock Text="{Binding Author.Username}" Foreground="Gray"/>
                                    <TextBlock Text="{Binding Rating, StringFormat=⭐ {0:F1}}"/>
                                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,10">
                                        <Button Content="Читать" Tag="{Binding Id}" Click="ReadBook_Click" Margin="2" Padding="8,4" Background="#3498DB" Foreground="White"/>
                                        <Button Content="В список" Tag="{Binding Id}" Click="AddToList_Click" Margin="2" Padding="8,4" Background="#F39C12" Foreground="White"/>
                                    </StackPanel>
                                </StackPanel>
                            </Border>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </ScrollViewer>
        </Grid>
    </Page>